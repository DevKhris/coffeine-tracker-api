import { JwtAuthGraphQlGuard } from './jwt-auth-graphql.guard';

describe('JwtAuthGraphQlGuard', () => {
  let jwtAuthGraphQlGuard: JwtAuthGraphQlGuard;
  it('should be defined', () => {
    expect(jwtAuthGraphQlGuard).toBeUndefined();
  });
});
